# 期中验收版本（暂定）

- 版本：**v0.2.0-alpha.1**
- 日期：2026-09-20
- 阶段：Alpha；期中 Demo 工程快照，标题中的“暂定”保留。
- 仓库：https://github.com/djwcb2333/GDATtest（私有）
- 工程入口：`GDATtest.uproject`，沿用 UE 5.8.2。
- 演示地图：`/Game/DoughWorld/Maps/Gameplay/Levels/L_DoughWorld_Map1`。

## 本次内容

1. 过场展示：水井引导、触发器、镜头往返、HUD 离返场、字幕与一次性完成进度；包含过场和正常游玩的独立镜头晃动配置。
2. 路标方向提示：多方向、可变数量、中英文文本、世界目标与屏幕方向投影。
3. 用户指定的期中 Demo 地图 Map1 当前已保存版本。项目启动仍沿用现有菜单流程；需要直接编辑地图时，在内容浏览器进入 DoughWorld → Maps → Gameplay → Levels → L_DoughWorld_Map1。
4. 本阶段的音乐、音效、Logo、图标、物品与交互显示调整；采集成功、失败和停止音分别支持五槽候选。

过场触发器和路标基础已包含于此前提交 `882fa25`。本次继续保存其后的全部 238 个工程文件变化：229 个资产、1 张地图、8 个源码文件，总文件大小约 80.3 MB；实际网络增量可能不同。

## 版本命名理由

相较 v0.1.0-alpha.2，这批新增功能和地图形成新的演示里程碑，次版本提升至 0.2.0，Alpha 序号从 1 开始。本版本没有被标为正式发行或课程最终验收通过；已发布旧标签保持原位。

## 验证与边界

- 之前功能任务记录：过场/路标 Editor 与 Game Development 编译通过，Map1 PIE 26/26、方向数量与视角 4/4、保存后场景审计 8/8；镜头晃动 PIE 40/40；采集音配置/选择检查 116 项通过。
- 本次发布核对：8 个变动源码文件 SHA256 与此前验收清单全部一致。以上是已有功能验收证据，不代表本次重新运行了所有测试。
- 当前地图与美术采用用户指定的已保存快照；本次不重新编译、运行、保存或重建 UE 资源，没有逐条复核课程评分要求，也没有验证独立运行、发行打包或另一台电脑首次构建。
- 采集成功音已有用户确认；失败/停止音的最终听感未新增验收。既有 GameFeatureData 资产规则提示仍是已知问题。
- 本次不修改存档结构、不删除或覆盖存档；发布前记录 15 份本机存档哈希，Saved 不纳入 Git。旧存档的完整游玩兼容性未在此次上传重新验证。

## 获取与后续迭代

有权限的队员使用 GitHub Desktop 克隆完整仓库，确保 Source、Content、Config 和 `.uproject` 一起取得，等待 LFS 下载完成；环境步骤见 README_GDATtest.md。不要只下载 Content。

后续在同一里程碑内修正问题，可使用 v0.2.0-alpha.2；不要移动本次已发布标签。日常先在 UE 保存，再 Commit，最后 Push origin。
