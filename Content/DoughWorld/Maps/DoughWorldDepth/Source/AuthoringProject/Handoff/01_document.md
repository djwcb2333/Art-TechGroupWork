请读取并执行这个完整包，为《面团世界》在Unreal中完成六区场景的纵深搭建。我需要实际落地的场景，不是仅给方案或生成物体清单。

先读00_开始这里.md，再读Design、Data与04_验收与交付.md。所有输入均在本包内。Originals是只读原件：保留原地图的分区关系、点位XY、路线与机关语义，在此基础上增加Z高差、断面、孔洞、地下空间、植被与环境模块。不要重画地图，不要导入旧V3/V4/V5的替代高度图、布局或实例数据。

先现场检查工程与实际引擎版本。工程候选路径为E:/Unreal Project/GDATtest/GDATtest.uproject，这只是定位线索，必须核实。检查当前场景、资产、材质、碰撞、角色、相机及已有建造/交互系统。创建独立候选关卡，建议/Game/Maps/DoughWorldDepth/Levels/L_DoughWorld_Depth；名称已存在时先确认归属，避免覆盖别的任务。共享材质需要修改时复制为候选专用版本，角色和输入保持既有约定。

固定相机方位、距离与不可缩放是硬要求。读取现有相机投影、旋转、FOV/OrthoWidth、SpringArm等并记录，不用旋转、自动拉远或缩放掩盖遮挡。参考PNG并非实际玩家视图，构图需从实际相机验证。

原PNG/JSON的差异按00中的默认规则处理并记录，不擅自创建折中的第三套坐标，不因局部资料差异停止所有工作。保留JSON中所有原有字段及ID；用独立增补记录保存新增Z、网格、材质、地形约束和实际对象路径。原JSON中的“Demo已实现”等描述必须现场验证。

先按Design/纵深设计.md完成原基地接近段和原黄油瀑布—冲撞破壁段两个高完成度样板。宽缓地面承载日常采集、战斗和建造；局部台缘、沟壁、孔洞与背景体量形成纵深。地形连续过渡，不要把区域做成平面色块、等高圆盘或随机山包。原XY无法容纳建议坡度时降低局部高差，不挪路线或建筑。高度图只能承载地表，真实孔洞、洞顶、悬挑、桥下必须有几何结构。

检查所有可用环境资产，按Data/module_requirements.json完成每一区每一类模块。缺失时搭对应部件、形态与尺寸的组合占位，至少有地表过渡、立面厚度、植被层级及地标体量。不可只把资产名列出来或用一个方块代表整片菌落。低草可实例化；需要淡出的菌盖、树冠、屋顶与前景墙片独立分件。原flora位置作组团锚点，新装饰不能改变资源点或挡住建造与通行空间。

Code是未编译组件候选。检查实际API和项目模块，完成编译、材质CameraFade接线与组件标签设置，再做单体、多层及同资产独立渐隐测试。它目前围绕角色检测，不自动覆盖任意交互目标；若当前交互对象仍被遮挡，应接入工程实际交互目标作为额外检测目标，或用局部构图解决，不能声称候选代码已经满足全部交互遮挡。不要改碰撞，不让整片实例化植被一起透明，不淡地形、危险孔边或强制门。

样板通过实际固定视角与PIE检查后继续完成其余区域，不将样板视为最终范围。原冲撞破壁及哨戒缝保持唯一入口；压力板是可选捷径，保留绕行；深渊需要主动交互确认，不能当作普通掉落陷阱。世界深层按地下空间实现，不因示意图在右上角就变成地表可走入的露天区域。未实现玩法可用清楚标记的交互/角色体量代理验证环境，但必须如实列出缺项，不编造已完成系统。

完成04列出的真实镜头、通行、建造、遮挡、边界及保存重载检查；提交候选关卡、逐类模块实际对象表、坐标增补表、源码改动、截图/视频、验证报告及未完成项。不要用离线图、编辑器自由镜头或编译成功替代游戏镜头与运行验证。保持执行直到本包要求范围完成；确有工具/工程阻塞时明确已完成与阻塞证据。
